LINUX-NTFS AKMOD — PROCÉDURE DE MAINTENANCE
=============================================

Projet :
    linux-ntfs-akmod-dev

Source amont :
    Namjae Jeon's linux-ntfs

Branch :
    ntfs-next


1. MISE À JOUR UPSTREAM
-----------------------

Le mécanisme automatisé est assuré par :

    tools/auto-update-ntfs-next.sh

Le script vérifie le commit amont ntfs-next.

Lorsqu'un nouveau commit est disponible, il :

    - récupère le nouveau commit ;
    - crée/récupère l'archive SOURCE ;
    - met à jour le SPEC ;
    - synchronise l'environnement rpmbuild ;
    - construit les RPM ;
    - identifie déterministiquement le RPM akmod ;
    - n'effectue aucune installation système ;
    - ne lance pas directement akmods.


2. IDENTIFICATION DU RPM AKMOD
------------------------------

L'identification du RPM akmod ne doit pas utiliser la date de
modification des fichiers.

Le script extrait du SPEC :

    AKMOD_NAME
    AKMOD_VERSION
    AKMOD_RELEASE
    AKMOD_ARCH

et construit :

    AKMOD_RPM="$RPMBUILD_TOPDIR/RPMS/$AKMOD_ARCH/$AKMOD_NAME-$AKMOD_VERSION-$AKMOD_RELEASE.$AKMOD_ARCH.rpm"

Le fichier est ensuite vérifié avant installation.

Cette identification déterministe est obligatoire pour
l'automatisation finale.


3. MISE À JOUR DU SPEC
----------------------

Modifier :

    SPECS/linux-ntfs-kmod.spec

Mettre à jour :

    Version
    Release
    Source
    %changelog

Chaque nouvelle version doit conserver une correspondance claire
entre :

    commit upstream
    archive SOURCE
    Version RPM
    Release RPM


4. CONSTRUCTION
---------------

Vérifier d'abord :

    /usr/bin/bash -n tools/auto-update-ntfs-next.sh
    /usr/bin/git diff --check

Construire :

    /usr/bin/rpmbuild -ba \
        "$HOME/rpmbuild/SPECS/linux-ntfs-kmod.spec"


5. AKMOD
---------

Installer séparément le RPM akmod produit par la construction, lorsque cette étape est souhaitée.

Puis vérifier :

    /usr/bin/rpm -q akmod-linux-ntfs

Si une génération immédiate du kmod pour le noyau courant est nécessaire, elle peut être demandée manuellement :

    sudo /usr/bin/akmods \
        --force \
        --akmod linux-ntfs \
        --kernels "$(/usr/bin/uname -r)"


6. VÉRIFICATION DU MODULE
-------------------------

    /usr/bin/modinfo ntfs
    /usr/bin/modinfo -F vermagic ntfs
    /usr/bin/modinfo -F filename ntfs

Vérifier également :

    /usr/bin/lsmod | /usr/bin/grep '^ntfs '


7. MONTAGES NTFS
----------------

    /usr/bin/findmnt -t ntfs,ntfs3

Les partitions utilisant le pilote linux-ntfs doivent apparaître
avec :

    FSTYPE ntfs

et non :

    FSTYPE ntfs3


8. AUTOMATISATION SYSTEMD
-------------------------

Les unités du projet sont :

    tools/systemd/linux-ntfs-next-update.service.in
    tools/systemd/linux-ntfs-next-update.timer
Installateur systemd utilisateur :
    tools/install-systemd-user.sh


Le timer actuel utilise :

    OnBootSec=15min
    OnUnitActiveSec=6h
    RandomizedDelaySec=30min
    Persistent=true

Vérifier :

    /usr/bin/systemctl --user status \
        linux-ntfs-next-update.timer \
        --no-pager

    /usr/bin/systemctl --user list-timers \
        linux-ntfs-next-update.timer \
        --no-pager

Le service doit terminer avec succès lorsqu'aucun nouveau commit
upstream n'est disponible.


9. RÈGLE IMPORTANTE
-------------------

Ne pas ajouter dans l'automatisation une recherche de RPM basée
sur :

    - la date de modification ;
    - -newer "$RPMBUILD_SPEC" ;
    - le dernier fichier trouvé dans RPMS/ ;
    - un glob ambigu pouvant correspondre à plusieurs RPM.

Le RPM akmod doit être identifié par les métadonnées du SPEC et
par le chemin déterministe AKMOD_RPM.


10. RESPONSABILITÉS
-------------------

L'orchestrateur est responsable de :

    - surveiller ntfs-next ;
    - récupérer la nouvelle source ;
    - mettre à jour le SPEC ;
    - construire le paquet ;
    - identifier le RPM akmod attendu ;
    - ne pas effectuer l'installation système ;
    - ne pas lancer directement akmods.

Akmods est responsable de :

    - construire le kmod pour les noyaux concernés ;
    - gérer les kmods associés aux différents noyaux ;
    - maintenir la compatibilité avec les mises à jour du noyau.

Le projet ne doit pas revenir à une compilation manuelle de
ntfs.ko après chaque mise à jour du noyau.


11. ÉTAT ACTUEL
---------------

État de référence actuellement validé :

Commit upstream :

    cbf02ac92f191fdb6c500c32072efedc1cac3a13

Commit :

    ntfs: do not update ctime when setxattr fails

Version :

    20260807

Release :

    5.fc44

Archive source :

    SOURCES/linux-ntfs-ntfs-next-cbf02ac9.tar.gz


VALIDATION FINALE
-----------------

Le mécanisme linux-ntfs-akmod a été testé et validé sur quatre
machines Fedora.

Les validations couvrent notamment :

    - synchronisation du dépôt Git avec GitHub ;
    - détection du commit ntfs-next suivi ;
    - absence de rebuild lorsque le commit upstream est inchangé ;
    - fonctionnement de l'orchestrateur automatique ;
    - fonctionnement du timer systemd utilisateur ;
    - construction et installation du paquet akmod ;
    - génération du kmod pour le noyau courant ;
    - chargement automatique du module ntfs ;
    - correspondance du module avec le noyau courant ;
    - fonctionnement avec Secure Boot et module signé ;
    - montage des partitions NTFS avec le pilote linux-ntfs ;
    - utilisation des options native_symlink=raw et symlink=wsl ;
    - absence de dépendance à ntfs-3g ;
    - vérification par akmods après mise à jour du noyau.

Validation représentative sur Legion :

    Noyau :
        7.1.8-200.fc44.x86_64

    Kernel-devel correspondant :
        kernel-devel-7.1.8-200.fc44.x86_64

    Paquet akmod :
        akmod-linux-ntfs-20260807-5.fc44.x86_64

    Module :
        /lib/modules/7.1.8-200.fc44.x86_64/extra/linux-ntfs/ntfs.ko.xz

    Vermagic :
        7.1.8-200.fc44.x86_64 SMP preempt mod_unload

    Signature :
        PKCS#7

    Service akmods :
        active (exited), status=0/SUCCESS

    Vérification akmods :
        Checking kmods exist for 7.1.8-200.fc44.x86_64 [ OK ]

    Module chargé :
        ntfs

    Partitions validées :
        /mnt/Autres
        /mnt/Utilitaires

    Type de système de fichiers :
        ntfs

    Options linux-ntfs validées :
        native_symlink=raw
        symlink=wsl


AUTOMATISATION UPSTREAM
-----------------------

Le timer systemd utilisateur vérifie périodiquement ntfs-next.

Lorsque le commit upstream est inchangé :

    - aucune archive source inutile n'est créée ;
    - aucun fichier n'est modifié ;
    - aucun nouveau build RPM n'est effectué ;
    - l'AKMOD et le module ntfs sont vérifiés par rapport au commit suivi.

Le mécanisme est donc considéré comme opérationnel et validé.

Le projet ne doit pas revenir à une compilation manuelle de
ntfs.ko après chaque mise à jour du noyau.
